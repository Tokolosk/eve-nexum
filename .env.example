# ── Database ──────────────────────────────────────────────────────────────────
PG_HOST=localhost
PG_PORT=5432
PG_DB=eve_nexum
PG_USER=nexum

# Generate a long random string: openssl rand -hex 32
PG_PASSWORD=

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=production

# Generate a long random string: openssl rand -hex 32
SESSION_SECRET=

# Encrypts EVE OAuth tokens at rest. 64 hex chars (32 bytes). Required in
# production. Generate: openssl rand -hex 32
# WARNING: rotating this key makes existing stored tokens unreadable (users
# will be forced to re-login on next ESI call).
TOKEN_ENCRYPTION_KEY=

# ── EVE SSO ───────────────────────────────────────────────────────────────────
# Register your app at https://developers.eveonline.com
EVE_CLIENT_ID=
EVE_CLIENT_SECRET=
# Must match the callback URL registered in your EVE developer app
EVE_CALLBACK_URL=https://yourdomain.com/auth/callback

# ── URLs ──────────────────────────────────────────────────────────────────────
# The public URL of the frontend (used for CORS and post-login redirect)
FRONTEND_URL=http://yourdomain.com

# Public hostname — used by docker-compose.traefik.yml for the Traefik router rule
DOMAIN=yourdomain.com

# Traefik router/service/middleware name prefix. Leave unset for a single
# deployment (defaults to eve-nexum). Only set this when running a SECOND
# instance on the same host/Traefik (e.g. STACK=eve-nexum-qa) so its Traefik
# names don't collide with the first. See QA-SETUP.md.
# STACK=eve-nexum

# ── Limits ────────────────────────────────────────────────────────────────────
# Max maps a single user can create (default: 5)
MAX_USER_MAPS=5
MAX_CORP_MAPS=5
MAX_ALLIANCE_MAPS=5

# ── ISK for extra maps (public/solo installs only) ────────────────────────────
# Lets pilots raise their OWN personal map limit by donating ISK to a
# corporation you nominate. Off by default, and ignored entirely when CORP_ID or
# ALLIANCE_ID is set — a corp or alliance deployment manages its own limits and
# never sees the option.
#
# Setup order matters:
#   1. Add esi-wallet.read_corporation_wallets.v1 to your EVE application and
#      wait for it to propagate.
#   2. Set the variables below and restart.
#   3. In Admin -> Maps, click "Connect wallet reader" and log in AS the
#      character named in ISK_MAPS_READER_CHAR_ID.
#
# The reader character must hold the in-game Accountant or Junior Accountant
# role in the recipient corporation, or ESI returns 403. The scope is requested
# only by that one admin flow — ordinary logins never ask for wallet access.
#
# Donations are read from the corp wallet journal, which EVE publishes hourly,
# so crediting takes up to an hour. Only entries newer than the moment you
# connect the reader are counted, and each is credited exactly once.
# ISK_MAPS_ENABLED=true
# ISK_MAPS_CORP_ID=98120330
# ISK_MAPS_READER_CHAR_ID=92961862
# Wallet division to read. Player donations land in division 1 (master).
# ISK_MAPS_DIVISION=1
# ISK per grant, and how many maps a grant is worth. Entitlement accumulates:
# two half payments earn a grant, and a double payment earns two.
# ISK_MAPS_PRICE=500000000
# ISK_MAPS_PER_GRANT=5
# ISK_MAPS_POLL_MINUTES=15

# ── SDE auto-update ───────────────────────────────────────────────────────────
# The server checks once a day for a new CCP SDE build and re-seeds only if it
# changed (a cheap HEAD request when nothing changed). On by default.
# SDE_AUTO_UPDATE=0      # set to 0 to disable the daily check
# SDE_CHECK_UTC=11:30    # HH:MM UTC; just after the 11:00 downtime export drop

# ── Demo map ──────────────────────────────────────────────────────────────────
# New users get a starter "Demo Map" seeded on their first login. On by default.
# INCLUDE_DEMO_MAP=0     # set to 0/false to give new users a blank canvas instead

# Read where each pilot's medical and jump clones are. Powers the Clones panel,
# and lets location tracking tell a clone jump from a flown jump so a death clone
# stops drawing a wormhole between the system you left and the one you woke in.
#
# ADD esi-clones.read_clones.v1 TO YOUR EVE APPLICATION FIRST, then set this.
# Requesting a scope the application doesn't have makes SSO reject every login
# with invalid_scope. Left off, both features stay dormant and nothing else
# changes.
# ESI_CLONES_SCOPE=true

# ── Dev only ──────────────────────────────────────────────────────────────────
# Set to http://server:3001 when running inside Docker dev compose
API_TARGET=http://localhost:3001


# ── Corp mode ─────────────────────────────────────────────────────────────────
# Comma-separated list of EVE corporation IDs that are allowed to log in. Any
# character whose corp is NOT in this list is rejected at the OAuth callback.
# Leave empty to disable corp mode entirely (anyone with EVE SSO can log in).
# Examples:
#   CORP_ID=98000001            # single corp
#   CORP_ID=98000001,98000002   # two corps share this deployment
CORP_ID=

# Character ID of the bootstrap admin. This character is forced to the top role
# on first login (alliance_admin when ALLIANCE_ID is set, otherwise admin) and
# can never be demoted or blocked by other admins — safety hatch against
# locking yourself out. Required whenever CORP_ID or ALLIANCE_ID is set.
ADMIN_CHAR_ID=

# Role NEW users are created with on a corp/alliance instance. Default 'readonly'
# (an admin then promotes them). Set to 'edit' to let every admitted member edit
# maps as soon as they log in ('full' is the tier just above edit). Existing
# users keep whatever role they already have. Only these editing tiers are
# accepted here — 'admin' / 'alliance_admin' can never be granted by default and
# must be assigned per-user by an admin; any other value falls back to 'readonly'.
DEFAULT_USER_ROLE=readonly

# Days an idle corp map can sit untouched before it's auto-archived (default: 30).
CORP_MAP_TIME=30

# Set to 1/true to share every corp map across every listed corp. Default
# (false / unset) scopes each corp map to its creating corp — Corp A's chain
# is invisible to Corp B even though they share the deployment. Only enable
# this when all listed corps explicitly trust each other.
CORP_MAP_SHARED=false

# ── Alliance mode ─────────────────────────────────────────────────────────────
# Comma-separated list of EVE alliance IDs allowed to log in. A character is
# admitted if their corp is in CORP_ID OR their alliance is in ALLIANCE_ID, so a
# whole alliance can be permitted without listing every member corp. Adds a third
# map scope (personal -> corp -> alliance) managed by the 'alliance_admin' role.
# Leave empty to disable alliance mode. Examples:
#   ALLIANCE_ID=99000001            # single alliance
#   ALLIANCE_ID=99000001,99000002   # coalition of two alliances
ALLIANCE_ID=

# Alliance counterpart of CORP_MAP_SHARED. Set to 1/true so every member of any
# listed alliance sees every alliance map (coalition mode). Default (false)
# scopes each alliance map to its owning alliance.
ALLIANCE_MAP_SHARED=false

# ── External API ──────────────────────────────────────────────────────────────
# Kill switch for the inbound external read API (/api/v1) — the Bearer-key REST
# + SSE surface third-party tools use to read your maps/systems/signatures. Set
# to 1/true to turn it off entirely (every /api/v1 request returns 403) without
# having to revoke issued keys. Default (false) leaves it enabled. Does not
# affect the app's own UI or outbound Discord/webhook pushes.
DISABLE_EXTERNAL_API=false

# ── Telemetry ─────────────────────────────────────────────────────────────────
# Opt-in anonymous deployment ping. OFF unless you opt in. When enabled the
# server sends a tiny daily request with the app version, a random per-instance
# id, and two aggregate counts (# maps, # users) — no character/corp/map detail,
# and the receiver stores no IP. It just lets the project gauge active self-hosted
# installs, versions, and rough scale.
#
# Two ways to opt in:
#   1. Set NEXUM_TELEMETRY=1 (sends to the project's default collector), OR
#   2. Set NEXUM_TELEMETRY_URL to any non-empty value — populating a collector
#      endpoint is itself treated as consent. Point it at the project's collector
#      or your own. Comment it out / leave it unset to stay opted out.
# NEXUM_TELEMETRY=1
# NEXUM_TELEMETRY_URL=https://eve-nexum.com/api/telemetry

# ── Live kill feed ────────────────────────────────────────────────────────────
# Opt-in. When enabled, a single server-side consumer reads zKillboard's R2Z2
# ephemeral feed and flags recent high-value kills on systems currently shown on
# live maps (pushed over the existing SSE stream). OFF unless KILL_FEED=1.
#
# zKillboard fair-use: keep KILL_FEED_CONTACT a reachable contact — it builds the
# feed's User-Agent, and a blank UA is Cloudflare-blocked.
# KILL_FEED=1
# KILL_FEED_CONTACT=you@example.com
# KILL_FEED_MIN_ISK=50000000        # only flag kills at/above this ISK value
# KILL_FEED_RECENT_SECONDS=900      # how long a flag lingers (client shows ~15m)
# KILL_FEED_BACKFILL_SECONDS=10800   # kill-log history window (default 3h)


